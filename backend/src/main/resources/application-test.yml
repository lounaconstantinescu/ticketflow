spring:
  datasource:
    url: jdbc:h2:mem:ticketflow_test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    driverClassName: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false

logging:
  level:
    root: WARN

ticketflow:
  seed:
    enabled: true
